#!/usr/bin/env bash

set -euo pipefail

CMD="python3 -m http.server"
PORT=8000
PATTERN="$CMD $PORT"

if pgrep -f "$PATTERN" > /dev/null; then
  echo "Server is running on port $PORT. Stopping it..."
  pkill -f "$PATTERN"
else
  echo "Server is not running."
fi
